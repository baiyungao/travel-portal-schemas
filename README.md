# travel-portal-schemas

Source-of-truth contracts for Travel Portal.

## Layout

- `schemas/` JSON Schemas consumed by web/api/planner via vendoring.
- `prompts/` versioned prompt templates, organized by domain and locale.
- `trust-registry/` curated trust source registry used by planning and trust features.
